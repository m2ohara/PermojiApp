<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="ring"
    android:innerRadius="30dp"
    android:thickness="10dp"
    android:useLevel="false" >
    <solid android:color="@color/user_profile_layout" />

    <stroke
        android:width="10dp"
        android:color="@color/user_profile_layout" />
</shape>